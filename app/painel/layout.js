export const metadata = {
  title: "CRM Honda",
  description: "Painel de leads e WhatsApp da Honda.",
  robots: { index: false, follow: false },
};

export default function PainelLayout({ children }) {
  return children;
}
