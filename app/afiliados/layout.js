export const metadata = {
  title: "CRM Afiliados",
  description: "Disparo de ofertas afiliadas no WhatsApp.",
  robots: { index: false, follow: false },
};

export default function AfiliadosLayout({ children }) {
  return children;
}
